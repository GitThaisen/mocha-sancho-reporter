describe('Component 2', function () {
  it('validates input', function () {});
  it('fetches data', function () {});

  describe('when data is fetched', function () {
    it('validates the data', function () {});
    it('maps the data', function () {});
    it('returns the data', function () {});
  });

  describe('if the request fails', function () {
    it('logs the error', function () {});
    it('maps and returns the error', function () {});
  });
});
